name: animate_text
description: "Helps you to animate text easy with scale, opacity, transform, blur. ចលនាអក្សរ"
version: 0.0.5
homepage: https://github.com/RathaIct/AnimateText
repository: https://github.com/RathaIct/AnimateText
issue_tracker: https://github.com/RathaIct/AnimateText/issues
maintainer: Hin Ratha (@RathaIct)
topics: [animate-text, animation-text, animated]
screenshots:
  - description: AnimateText
    path: thumbnail.gif

environment:
  sdk: ">=3.3.2 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
